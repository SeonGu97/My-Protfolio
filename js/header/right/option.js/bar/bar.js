'ues strict';

export default class Bar {
    constructor(creator, data, loop) {
        loop(creator, data[9], data[7].name);

        data[9].name.addEventListener('click', e => {
            
        });
    }
}